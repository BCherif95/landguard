spring:
  datasource:
    url: jdbc:mysql://mysql:3306/laboussole?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DATASOURCE_USERNAME:laboussole}
    password: ${DATASOURCE_PASSWORD:laboussole}

laboussole:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
