# LA BOUSSOLE — frontend environment.
# Copy to .env.local before running `pnpm dev`.

NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Mapbox token — required once the satellite map module lands.
# Free tier: https://account.mapbox.com/access-tokens/
# NEXT_PUBLIC_MAPBOX_TOKEN=
